# TODO: Perbaikan halaman Edit Profil

## Langkah
- [ ] Periksa kode `EditProfileView` & `EditProfileController`.
- [ ] Perbaiki error `Unable to load asset: "AssetManifest.json"` (pastikan asset diakses benar dan build benar).
- [ ] Pastikan perubahan nama benar-benar menulis ke Firestore field `users/{uid}.nama`.
- [ ] Pastikan halaman Profil membaca data terbaru setelah update (hindari data lama yang belum ter-refresh).
- [ ] Jalankan `flutter analyze` dan build ulang untuk verifikasi.

